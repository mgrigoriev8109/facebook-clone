<h1>News Feed</h1>

<p>Make a post!</p>
<%= render 'new_post' %>

<ul>
  <% unless @posts.nil? %>
    <% @posts.each do |post| %>
      <li>
        <p>Author: <%= User.find(post.user_id).username %></p>
        <p>Post: <%= post.body %></p>
      </li>
    <% end %>
  <% end %>
</ul>

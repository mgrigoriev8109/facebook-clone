<h1>Friends:</h1>
<ul>
<% @friendships.each do |friendship| %>
  <% if request.friendship_provider_id == current_user.id %>
    <% user = User.find(request.friendship_recipient_id) %>
    <li><%= link_to user.username, user %></li>
  <% elsif request.friendship_recipient_id == current_user.id %>
    <% user = User.find(request.friendship_provider_id) %>
    <li><%= link_to user.username, user %></li>
  <% end %>
<% end %>
</ul>
